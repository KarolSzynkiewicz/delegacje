server {
    listen 0.0.0.0:8080;
    server_name _;
    
    access_log /dev/stdout;
    error_log /dev/stderr;
    
    location / {
        return 200 "OK";
        add_header Content-Type text/plain;
    }
}
