server {
    listen 0.0.0.0:${PORT};
    server_name _;
    
    access_log /dev/stdout;
    error_log /dev/stderr;
    
    location / {
        return 200 "OK\n";
        add_header Content-Type text/plain;
    }
}
